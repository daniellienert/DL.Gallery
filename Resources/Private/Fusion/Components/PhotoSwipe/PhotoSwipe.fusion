prototype(DL.Gallery:Components.PhotoSwipe) < prototype(Neos.Fusion:Template) {
    templatePath = ${'resource://DL.Gallery/Private/Fusion/Components/PhotoSwipe/PhotoSwipe.html'}
    @if.formatIsHtml = ${request.format == 'html'}
}
